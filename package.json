
{
  "name": "voucher-data-hub",
  "version": "1.3.0",
  "private": true,
  "description": "Secure Voucher Distribution Tracking System",
  "scripts": {
    "start": "npx serve -s .",
    "build": "npx esbuild index.tsx --bundle --minify --outfile=index.js --format=esm --target=esnext --external:react --external:react-dom --external:recharts --external:lucide-react --external:@supabase/supabase-js --define:process.env.SUPABASE_URL=\"\\\"$SUPABASE_URL\\\"\" --define:process.env.SUPABASE_ANON_KEY=\"\\\"$SUPABASE_ANON_KEY\\\"\""
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
